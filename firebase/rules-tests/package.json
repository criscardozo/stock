{
  "name": "rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-stock --config ../firebase.json \"vitest run\"",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@types/node": "^26.2.0",
    "firebase": "^12.17.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
